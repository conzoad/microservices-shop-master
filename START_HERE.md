# 🎉 Проект настроен для запуска в Docker!

## ✅ Что было сделано

### 1. Создана Docker инфраструктура

- ✅ **docker-compose.yml** - оркестрация всех сервисов для development
- ✅ **docker-compose.prod.yml** - конфигурация для production (с Gunicorn, Nginx)
- ✅ **Dockerfile** для каждого микросервиса (6 штук)
- ✅ **.dockerignore** файлы для оптимизации сборки

### 2. Обновлены конфигурации приложения

- ✅ **shared/utils.py** - поддержка переменных окружения для Redis и service URLs
- ✅ **api-gateway/config/settings.py** - настройки для Docker
- ✅ **services/user-service/config/settings.py** - поддержка Docker
- ✅ **frontend/vite.config.js** - host: '0.0.0.0' для Docker network

### 3. Созданы скрипты инициализации

- ✅ **init.ps1** - PowerShell скрипт для Windows
- ✅ **init.sh** - Bash скрипт для Linux/Mac

### 4. Написана документация

- ✅ **DOCKER_GUIDE.md** - полное руководство (команды, диагностика, troubleshooting)
- ✅ **QUICKSTART.md** - быстрый старт (3 команды)
- ✅ **CHECKLIST.md** - чек-лист перед запуском
- ✅ **DOCKER_SETUP_SUMMARY.md** - описание архитектуры
- ✅ **README.md** - обновлен с информацией о Docker
- ✅ **.env.example** - пример конфигурации

---

## 🚀 Как запустить проект

### Шаг 1: Убедитесь, что Docker Desktop запущен

Проверьте, что в трее Windows есть иконка Docker и она активна.

### Шаг 2: Откройте PowerShell в папке проекта

```powershell
cd C:\Users\suhom\Downloads\microservices-shop-master
```

### Шаг 3: Запустите инициализацию

```powershell
.\init.ps1
```

**Или вручную:**
```powershell
docker-compose up -d --build
```

### Шаг 4: Откройте браузер

http://localhost:3000

**Учетные данные:**
- Email: `admin@shop.com`
- Password: `admin123`

---

## 📦 Архитектура Docker

### Запущенные контейнеры

| Контейнер | Порт | Описание |
|-----------|------|----------|
| shop-frontend | 3000 | Vue.js приложение |
| shop-api-gateway | 8000 | API Gateway |
| shop-user-service | 8004 | Сервис пользователей |
| shop-product-service | 8001 | Сервис товаров |
| shop-cart-service | 8002 | Сервис корзины |
| shop-order-service | 8003 | Сервис заказов |
| shop-redis | 6379 | Redis (event bus) |

### Docker Network

Все контейнеры находятся в одной сети `shop-network` и могут общаться друг с другом по именам:
- `http://user-service:8004`
- `http://product-service:8001`
- и т.д.

### Volumes (Постоянное хранилище)

- `user-db` - база пользователей
- `product-db` - база товаров
- `cart-db` - база корзин
- `order-db` - база заказов

Данные **сохраняются** между перезапусками контейнеров!

---

## 🔧 Полезные команды

### Просмотр статуса

```powershell
docker-compose ps
```

### Просмотр логов

```powershell
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f user-service
docker-compose logs -f frontend
docker-compose logs -f api-gateway
```

### Остановка проекта

```powershell
# Остановить контейнеры (данные сохранятся)
docker-compose down

# Остановить и удалить volumes (ОЧИСТИТ БАЗЫ ДАННЫХ!)
docker-compose down -v
```

### Перезапуск сервиса

```powershell
docker-compose restart user-service
```

### Пересборка конкретного сервиса

```powershell
docker-compose up -d --build user-service
```

### Выполнение команд внутри контейнера

```powershell
# Создать суперпользователя
docker-compose exec user-service python manage.py createsuperuser

# Django shell
docker-compose exec user-service python manage.py shell

# Bash в контейнере
docker-compose exec user-service /bin/bash
```

---

## 🐛 Устранение проблем

### Проблема: Docker Desktop не запущен

**Симптом:**
```
error during connect: ... Is the docker daemon running?
```

**Решение:**
1. Запустите Docker Desktop
2. Дождитесь, пока иконка в трее станет зеленой
3. Попробуйте снова

### Проблема: Порты заняты

**Симптом:**
```
Bind for 0.0.0.0:3000 failed: port is already allocated
```

**Решение:**
```powershell
# Проверьте, что занимает порт
netstat -ano | findstr "3000"

# Остановите процесс или измените порт в docker-compose.yml
```

### Проблема: Контейнер постоянно перезапускается

**Решение:**
```powershell
# Посмотрите логи
docker-compose logs <service-name>

# Пересоберите контейнер
docker-compose up -d --build <service-name>
```

### Проблема: Нет связи между сервисами

**Решение:**
```powershell
# Проверьте сеть
docker network inspect microservices-shop-master_shop-network

# Перезапустите весь проект
docker-compose down
docker-compose up -d
```

---

## 📖 Дополнительные ресурсы

- **Полное руководство:** [DOCKER_GUIDE.md](DOCKER_GUIDE.md)
- **Быстрый старт:** [QUICKSTART.md](QUICKSTART.md)
- **Чек-лист:** [CHECKLIST.md](CHECKLIST.md)
- **Описание архитектуры:** [DOCKER_SETUP_SUMMARY.md](DOCKER_SETUP_SUMMARY.md)

---

## 🎯 Следующие шаги

### Для разработки

1. Код обновляется автоматически (hot reload включен)
2. Frontend: изменения применяются моментально (Vite HMR)
3. Backend: изменения применяются при сохранении файла

### Для production

1. Используйте `docker-compose.prod.yml`
2. Настройте SSL сертификаты
3. Замените SQLite на PostgreSQL
4. Настройте CI/CD (GitHub Actions, GitLab CI)

---

## 💡 Советы

✅ Всегда запускайте через `docker-compose`, а не отдельные `docker run`  
✅ Используйте `docker-compose logs -f` для отладки  
✅ Volumes сохраняют данные - используйте `down -v` для полной очистки  
✅ Redis автоматически настроен для event bus между сервисами  

---

## 🎉 Готово!

Теперь весь проект полностью работает в Docker:
- ✅ Все сервисы изолированы
- ✅ Автоматическая настройка сети
- ✅ Постоянное хранилище данных
- ✅ Удобный запуск одной командой
- ✅ Production-ready конфигурация

**Запустите Docker Desktop и выполните:**
```powershell
.\init.ps1
```

**Откройте:** http://localhost:3000

Приятной работы! 🚀
