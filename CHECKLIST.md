# ✅ Чек-лист перед запуском

## Что нужно сделать

### 1. Установить Docker Desktop (если еще не установлен)

- Скачать: https://www.docker.com/products/docker-desktop/
- Установить и запустить Docker Desktop
- Убедиться, что в трее появилась иконка Docker

### 2. Проверить, что Docker запущен

```powershell
docker --version
docker info
```

Если `docker info` возвращает ошибку - запустите Docker Desktop.

### 3. Перейти в директорию проекта

```powershell
cd C:\Users\suhom\Downloads\microservices-shop-master
```

### 4. Запустить проект

**Простой способ:**
```powershell
.\init.ps1
```

**Или вручную:**
```powershell
docker-compose up -d --build
```

### 5. Дождаться запуска (1-2 минуты)

Проверить статус:
```powershell
docker-compose ps
```

Все контейнеры должны быть в статусе `Up`.

### 6. Открыть приложение

http://localhost:3000

## Если что-то пошло не так

### Проблема: Docker Desktop не запускается

**Решение:**
- Перезагрузите компьютер
- Проверьте, включена ли виртуализация в BIOS
- Запустите PowerShell от имени администратора

### Проблема: Порты заняты

**Решение:**
```powershell
# Проверить, какие порты заняты
netstat -ano | findstr "3000"
netstat -ano | findstr "8000"

# Остановить локальные серверы или изменить порты в docker-compose.yml
```

### Проблема: Контейнеры не запускаются

**Решение:**
```powershell
# Посмотреть логи
docker-compose logs

# Пересобрать с нуля
docker-compose down -v
docker-compose up -d --build
```

### Проблема: Frontend не открывается

**Решение:**
```powershell
# Проверить логи фронтенда
docker-compose logs frontend

# Перезапустить
docker-compose restart frontend
```

## Полезные команды

```powershell
# Остановить все контейнеры
docker-compose down

# Остановить и удалить данные
docker-compose down -v

# Перезапустить конкретный сервис
docker-compose restart user-service

# Зайти внутрь контейнера
docker-compose exec user-service /bin/bash

# Просмотреть логи
docker-compose logs -f

# Создать суперпользователя
docker-compose exec user-service python manage.py createsuperuser
```

## Структура портов

| Сервис | Порт | URL |
|--------|------|-----|
| Frontend | 3000 | http://localhost:3000 |
| API Gateway | 8000 | http://localhost:8000 |
| User Service | 8004 | http://localhost:8004 |
| Product Service | 8001 | http://localhost:8001 |
| Cart Service | 8002 | http://localhost:8002 |
| Order Service | 8003 | http://localhost:8003 |
| Redis | 6379 | localhost:6379 |

## Первый вход

После запуска через `init.ps1`:
- Email: `admin@shop.com`
- Password: `admin123`

## Следующие шаги

1. ✅ Запустить Docker Desktop
2. ✅ Выполнить `.\init.ps1`
3. ✅ Открыть http://localhost:3000
4. ✅ Войти под admin@shop.com
5. 🎉 Готово!
