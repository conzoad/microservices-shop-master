# 📦 Docker Configuration Summary

## Созданные файлы для Docker

### Основные файлы конфигурации

1. **docker-compose.yml** - Development конфигурация
   - Запуск всех микросервисов
   - Volumes для hot-reload
   - Настройка сети и зависимостей

2. **docker-compose.prod.yml** - Production конфигурация
   - Gunicorn вместо dev server
   - Nginx reverse proxy
   - Оптимизация производительности

### Dockerfile для каждого сервиса

- `api-gateway/Dockerfile` - API Gateway
- `services/user-service/Dockerfile` - User Service
- `services/product-service/Dockerfile` - Product Service
- `services/cart-service/Dockerfile` - Cart Service
- `services/order-service/Dockerfile` - Order Service
- `frontend/Dockerfile` - Vue.js Frontend

### .dockerignore файлы

- `api-gateway/.dockerignore`
- `services/user-service/.dockerignore`
- `frontend/.dockerignore`

### Скрипты инициализации

- `init.ps1` - PowerShell скрипт для Windows
- `init.sh` - Bash скрипт для Linux/Mac

### Документация

- `DOCKER_GUIDE.md` - Полное руководство по Docker
- `QUICKSTART.md` - Быстрый старт
- `CHECKLIST.md` - Чек-лист перед запуском
- `.env.example` - Пример переменных окружения

## Обновленные файлы

### Конфигурации сервисов

1. **shared/utils.py**
   - Добавлены переменные окружения для Redis
   - Поддержка Docker network

2. **api-gateway/config/settings.py**
   - ALLOWED_HOSTS включает Docker контейнеры
   - URL сервисов из переменных окружения
   - Redis cache конфигурация

3. **services/user-service/config/settings.py**
   - ALLOWED_HOSTS для Docker
   - Гибкая конфигурация БД

4. **frontend/vite.config.js**
   - host: '0.0.0.0' для доступа из других контейнеров
   - Прокси через переменную окружения

5. **readme.md**
   - Добавлена секция о Docker запуске

## Архитектура Docker

```
┌─────────────────────────────────────────────────┐
│              Docker Network                     │
│            (shop-network)                       │
│                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │  Redis   │  │ Frontend │  │   API    │     │
│  │  :6379   │  │  :3000   │  │ Gateway  │     │
│  └──────────┘  └──────────┘  │  :8000   │     │
│                               └────┬─────┘     │
│                                    │           │
│       ┌────────────────┬───────────┼─────┐     │
│       ▼                ▼           ▼     ▼     │
│  ┌─────────┐   ┌──────────┐   ┌──────────┐   │
│  │  User   │   │ Product  │   │   Cart   │   │
│  │ Service │   │ Service  │   │ Service  │   │
│  │  :8004  │   │  :8001   │   │  :8002   │   │
│  └────┬────┘   └────┬─────┘   └────┬─────┘   │
│       │             │              │          │
│  ┌────▼────┐   ┌────▼─────┐   ┌────▼─────┐   │
│  │ Volume  │   │ Volume   │   │ Volume   │   │
│  │user-db  │   │product-db│   │ cart-db  │   │
│  └─────────┘   └──────────┘   └──────────┘   │
│                                                │
│  ┌──────────┐                                 │
│  │  Order   │                                 │
│  │ Service  │                                 │
│  │  :8003   │                                 │
│  └────┬─────┘                                 │
│       │                                        │
│  ┌────▼─────┐                                 │
│  │ Volume   │                                 │
│  │ order-db │                                 │
│  └──────────┘                                 │
└─────────────────────────────────────────────────┘
```

## Volumes (Постоянные данные)

- `user-db` - База данных пользователей
- `product-db` - База данных товаров
- `cart-db` - База данных корзин
- `order-db` - База данных заказов

Данные сохраняются между перезапусками контейнеров.

## Порты (Host -> Container)

| Host | Container | Сервис |
|------|-----------|--------|
| 3000 | 3000 | Frontend |
| 8000 | 8000 | API Gateway |
| 8001 | 8001 | Product Service |
| 8002 | 8002 | Cart Service |
| 8003 | 8003 | Order Service |
| 8004 | 8004 | User Service |
| 6379 | 6379 | Redis |

## Переменные окружения

Все сервисы настроены через переменные окружения в `docker-compose.yml`:

- `REDIS_HOST` - Хост Redis (redis)
- `REDIS_PORT` - Порт Redis (6379)
- `DEBUG` - Режим отладки (True/False)
- `*_SERVICE_URL` - URL сервисов для межсервисного взаимодействия

## Health Checks

Redis имеет health check для обеспечения правильного порядка запуска сервисов.

## Команды Docker

```powershell
# Запуск
docker-compose up -d

# Сборка
docker-compose build

# Остановка
docker-compose down

# Логи
docker-compose logs -f

# Статус
docker-compose ps

# Перезапуск
docker-compose restart <service>

# Выполнение команд
docker-compose exec <service> <command>
```

## Следующие шаги

1. ✅ Запустить Docker Desktop
2. ✅ Выполнить `.\init.ps1` или `docker-compose up -d --build`
3. ✅ Дождаться запуска всех сервисов
4. ✅ Открыть http://localhost:3000
5. 🎉 Начать работу!

---

**Готово!** Проект полностью настроен для работы в Docker.
